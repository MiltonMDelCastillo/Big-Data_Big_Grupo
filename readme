requerimientos 

kafka-python==2.0.2
pandas==2.0.3
flask==2.3.3
flask-cors==4.0.0

paho-mqtt==1.6.1 // para MQTT  

//intalacion 
🔧 INSTALACIÓN INDIVIDUAL DE CADA DEPENDENCIA
1. Instalar kafka-python
bash
pip install kafka-python
2. Instalar pandas
bash
pip install pandas
3. Instalar flask
bash
pip install flask
4. Instalar flask-cors
bash
pip install flask-cors

pip install paho-mqtt
----------------------------------

🚀 ORDEN DE EJECUCIÓN:
bash
# Terminal 1 - Iniciar Kafka (desde la carpeta infra/)
cd infra
docker-compose up -d

# Terminal 2 - Ingestionar datos (desde carpeta principal)
cd csv-producers
python producer_sensores.py

# Terminal 3 - Ejecutar API (desde carpeta principal)  
cd csv-producers
python api_sensors.py
-----------------------------------------
para MQTT
cd infra
docker-compose -f docker-compose-mqtt.yml up -d